package com.ssukssuk.repository.plant;

import com.ssukssuk.domain.plant.CharacterCode;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CharacterCodeRepository extends JpaRepository<CharacterCode, Integer> {
}